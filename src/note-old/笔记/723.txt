json jquery vue

        var i={
        name:xxx,
        age:xxx
        };    //这样写相当于直接定义对象

        先css 再script 再html

<script>
    function main(){
            var mydiv=document.getElemtById("mydiv");
            console.log(mydiv)
            }
</script>
        写事件
        可以<body onload="main()">
<div id="mydiv"></div>
</body>

        netty

        File--->new project--->java Enterprise--->Application server选自己的tomcat--->Next--->
        creat project前面打勾
        不需要配任何环境变量！

        netstat-anlp|grep 8080监听端口号是否打开

        正常标签 只有Get方法
<a href="/xxx?name=x">
        String str=request.getParameter("name");

        爬站
        用正则表达式 爬所有的a标签

        getRequestDispatch 是调用了下一个servlet 写了一个页面out.print到当前页了

        sendirect 页面重定向是向页面发送3 的请求，有返回值，然后再连接网页200
        找到所有的标签这一行内容
        析取href字段
        发送Get请求

        base64

        编码

        URL

        var str="{id =122}"不加引号是对象，加了是字符串
        str=eval("("+str+")");eval动态解码

